<IfModule mod_rewrite.c>
  RewriteEngine On
</IfModule>

<IfModule mod_rewrite.c>
RewriteCond $1 !^(index\.php|assets|css|js|downloads|favicon\.ico)
RewriteRule ^(.*)$ index.php/?action=$1 [L]
</IfModule>
